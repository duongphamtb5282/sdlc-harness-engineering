# Agent-Generated Documents — Multi-Tenant Healthcare SaaS

This folder contains the **full documentation set generated by the agent** for the **Multi-Tenant Healthcare SaaS** system — produced from `documents/saas.md` through the kernel pipeline:

```
/discover → /spec → /arch-design → /plan → /qa
```

## Pipeline & Artifacts

| Stage | Command | Artifact |
|---|---|---|
| Discovery | `/discover` | `ideas/saas.md` — problem, assumptions (with validation gates), risks (owner + early-warning signal), cost & effort, roadmap |
| Specification | `/spec` | `SPEC.md` — objectives, TDD user stories with acceptance criteria, technical boundaries |
| Architecture | `/arch-design` | `adr/0001–0006-saas-*.md` — 6 decision records with alternatives and consequences |
| Architecture | `/arch-design` | `trade-offs/saas-trade-offs.md` — decision ledger: weighted options, revisit triggers, cross-decision effects |
| Architecture | `/arch-design` | `architecture/saas.md` — C4 diagrams, components, data model, API contracts, security |
| Planning | `/plan` | `tasks/plan.md` + `tasks/todo.md` — dependency graph, 4-sprint roadmap, 66 ordered tasks |
| QA | `/qa` | `qa/test-cases.md` — 52 test cases mapped to acceptance criteria (unit/API/E2E) |

## Flow

```
Discovery → Spec → Architecture (ADRs + trade-offs + design) → Plan → QA test cases → Build → Review
ideas/ → SPEC → adr/ + trade-offs/ + architecture/ → tasks/ → qa/ → code + tests → review report
```

**Source:** `documents/saas.md` (Multi-Tenant Healthcare SaaS — full design: market research, PRD, ADRs, C4, user stories, cost estimates).

*Generated 2026-07-31 by the Nexus Agent Kernel.*
