{
  "description": "Cost control hook — task complexity classification and model switching gate",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/cost-controller/classify-task.sh\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
