name: greenfield-nestjs
description: NestJS + PostgreSQL greenfield project template
stack:
  runtime: nodejs-22
  framework: nestjs-11
  database: postgresql-16
  orm: prisma
  api: [rest, graphql]
  auth: jwt
  testing: jest
  ci: github-actions
  deploy: [aws-ecs, azure-container-apps, gcp-cloud-run]
