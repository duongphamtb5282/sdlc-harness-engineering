# Java Spring — Scaffold Layout (SA Phase 6)

Generate Gradle Kotlin DSL project with packages:

- `{basePackage}.config`
- `{basePackage}.domain`
- `{basePackage}.application`
- `{basePackage}.infrastructure`
- `{basePackage}.api`

Include: `application.yml`, Flyway `V1__init.sql`, health actuator, springdoc OpenAPI stub, sample `*Controller` + `*Service` + `*Repository`.

Do not generate business logic — skeleton only.
